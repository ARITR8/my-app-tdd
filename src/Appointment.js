import React from 'react';

// Correctly define the Appointment component to render a JSX element
export const Appointment = () => {
    return <div>Ashley</div>; // Wrap the string in a <div> or any other element
};